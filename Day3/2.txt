一、实验介绍
1. 实验目的

• 理解ADC的工作原理

• 掌握STM32的ADC驱动写法

• 理解MQ2传感器功能及原理

• 掌握MQ2编程读取方法

2. 实验内容

MQ2是燃气传感器系列中的一款，可方便检测甲烷、丁烷、液化石油气、烟雾等浓度，AU100开发板带有燃气传感器底座，可安装多种燃气传感器。本实验通过在AU100开发板上安装MQ2传感器，并使用ADC模数转换实现MQ2浓度电压值的读取。通过本实验可了解MQ2使用方法，深入掌握ADC工作原理，掌握STM32的ADC驱动的写法，实验内容主要如下：

1） MQ2工程创建

2） MQ2驱动实现

3） MQ2测试示列实现

3. 实验知识点

• MQ2传感器工作原理

• STM32的ADC编程

• STM32的ADC工作原理

4. 实验时长

共2学时，学时分配如下：

• MQ2工程创建（0.5学时）

• MQ2驱动实现（1学时）

• MQ2测试示例实现（0.5学时）

5. 实验环境

• AU100开发板-STM32F407

• Keil MDK 5.36

• Keil.STM32F4xx_DFP.2.15.0

• STM32F4xx_StdPeriph_Driver v1.81

• Windows 10

二、实验分析
1）MQ2工程创建

创建驱动文件
调试串口添加
 2）MQ2驱动实现

GPIO配置
ADC初始化
ADC数据读取
3）MQ2测试示例实现

创建主程序
测试示例实现
三、实验步骤
MQ2工程创建
1） 创建驱动文件

编写MQ2驱动前，需首先创建工程目录，在Keil中创建相应驱动文件，以STM32工程模板为基础，添加相关文件，目录如图：



图 1 MQ2工程目录

BSP目录主要有:

mq.h    //mq对外接口头文件   
mq.c    //mq驱动实现源码
usart.h        //调试串口UART2对外接口头文件
usart.c        //调试串口UART2驱动实现源码
mq.h 头文件包含MQ2模块可对外提供的函数， 具体功能相关的定义在实验后续步骤中逐步添加，头文件内容如下：

【代码7-1-1】BSP/mq.h

#ifndef __MQ_H__
#define    __MQ_H__

#include "stm32f4xx.h"

void MQ_Init(void);
uint16_t MQ_ReadValue(void);

#endif /* __MQ_H__ */
mq.c为MQ2模块的源码实现，首先需定义一些全局量以方便功能实现，主要为头文件引用、 全局变量等内容，此处先做整体定义，具体作用后续步骤逐一说明。

【代码7-1-2】BSP/mq.c

#include "stm32f4xx.h"
#include "mq.h"
#include "delay.h"

#define MQ_ADC  ADC1
2） 创建调试串口

AU100使用UART2为调试串口，需先实现驱动文件。

usart.h为STM32的UART2调试串口的接口头文件，内容如下：

【代码7-1-3】BSP/usart.h

#ifndef __USART_H__
#define __USART_H__
#include "stm32f4xx.h"

void UART2_Init(u32 bound);

#endif
usart.c为STM32的UART2调试串口驱动实现源码，内容如下：

【代码7-1-4】BSP/usart.c

#include <stdio.h>
#include "usart.h"     

void UART2_Init(u32 bound)
{
    GPIO_InitTypeDef GPIO_InitStructure;
    USART_InitTypeDef USART_InitStructure;
    NVIC_InitTypeDef NVIC_InitStructure;
    
    //使能GPIOA和USART1时钟
    RCC_AHB1PeriphClockCmd(RCC_AHB1Periph_GPIOA,ENABLE);  //GPIOA在AHB1高速总线上
    RCC_APB1PeriphClockCmd(RCC_APB1Periph_USART2,ENABLE); //USART2在APB1硬件总线上

    //USART2 对应引脚复用映射
    GPIO_PinAFConfig(GPIOA, GPIO_PinSource2, GPIO_AF_USART2); 
    GPIO_PinAFConfig(GPIOA, GPIO_PinSource3, GPIO_AF_USART2); 
    
    //USART2 IO配置
    GPIO_InitStructure.GPIO_Pin  = GPIO_Pin_2 | GPIO_Pin_3; 
    GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF;     //复用
    GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;//速度50MHz
    GPIO_InitStructure.GPIO_OType = GPIO_OType_PP;   //推挽复用输出
    GPIO_InitStructure.GPIO_PuPd  = GPIO_PuPd_UP;    
    GPIO_Init(GPIOA,&GPIO_InitStructure);  
    
    //USART2 端口配置
    USART_InitStructure.USART_BaudRate = bound;  //波特率设置
    USART_InitStructure.USART_WordLength = USART_WordLength_8b;  //字长8bit
    USART_InitStructure.USART_StopBits   = USART_StopBits_1;       //1停止位
    USART_InitStructure.USART_Parity     = USART_Parity_No;          //无校验
    USART_InitStructure.USART_HardwareFlowControl = USART_HardwareFlowControl_None;//无硬件数据流控制
    USART_InitStructure.USART_Mode = USART_Mode_Rx | USART_Mode_Tx;    //收发
    USART_Init(USART2, &USART_InitStructure); 
    
    NVIC_InitStructure.NVIC_IRQChannel = USART2_IRQn;         //USART2中断向量
    NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = 3; //抢占优先级3
    NVIC_InitStructure.NVIC_IRQChannelSubPriority = 3;          //子优先级3
    NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;              //IRQ通道使能
    NVIC_Init( &NVIC_InitStructure );    
    
    USART_ITConfig(USART2, USART_IT_RXNE, ENABLE);//开中断
    USART_Cmd(USART2, ENABLE);  //使能串口2 
}


/*
发送一个字符 
*/
void UART_SendByte( USART_TypeDef * pUSARTx, uint8_t ch)
{
    //发送一个字节数据到USART
    USART_SendData(pUSARTx, ch);
        
    //等待发送数据寄存器为空
    while (USART_GetFlagStatus(pUSARTx, USART_FLAG_TXE) == RESET);    
}

/*
发送字符串
*/
void UART_SendString( USART_TypeDef * pUSARTx, char *str)
{
    unsigned int k=0;
    do {
      UART_SendByte( pUSARTx, *(str + k) );
      k++;
    } while(*(str + k)!='\0');
  
  //等待发送完成
  while(USART_GetFlagStatus(pUSARTx,USART_FLAG_TC)==RESET){}
}

/*****************  发送一个16位数 **********************/
void UART_SendHalfWord( USART_TypeDef * pUSARTx, uint16_t ch)
{
    uint8_t temp_h, temp_l;    
    temp_h = (ch&0XFF00)>>8;      /* 取出高八位 */
    temp_l = ch&0XFF;      /* 取出低八位 */
    
    USART_SendData(pUSARTx,temp_h);         /* 发送高八位 */
    while (USART_GetFlagStatus(pUSARTx, USART_FLAG_TXE) == RESET);    
    
    USART_SendData(pUSARTx,temp_l);         /* 发送低八位 */
    while (USART_GetFlagStatus(pUSARTx, USART_FLAG_TXE) == RESET);    
}

//重定向c库函数printf到串口，重定向后可使用printf函数
int fputc(int ch, FILE *f)
{
    // 发送一个字节数据到串口
    USART_SendData(USART2, (uint8_t) ch);
        
    // 等待发送完毕
    while (USART_GetFlagStatus(USART2, USART_FLAG_TXE) == RESET);        

    return (ch);
}

//重定向c库函数scanf到串口，重写向后可使用scanf、getchar等函数
int fgetc(FILE *f){
    // 等待串口输入数据
    while (USART_GetFlagStatus(USART2, USART_FLAG_RXNE) == RESET);
    return (int)USART_ReceiveData(USART2);
}


/*
串口2中断处理函数  
*/
void USART2_IRQHandler( void ){
    u8 sbuf = 0;
  
    //接收中断服务函数
    if(USART_GetFlagStatus(USART2, USART_FLAG_RXNE)) {
        sbuf = USART_ReceiveData(USART2);//收到一个字节
        USART_SendData(USART2, sbuf);    //回显转发回去
    }
}
MQ2驱动实现
1） GPIO配置

MQ2与PC2引脚连接，需先对GPIO进行初始化，实现源码如下：

【代码7-2-1】BSP/mq.c

void MQ_GPIO_Config(void){
    GPIO_InitTypeDef GPIO_InitX;    
    RCC_AHB1PeriphClockCmd(RCC_AHB1Periph_GPIOC, ENABLE);//使能GPIO 时钟

    //配置为模拟输入
    GPIO_InitX.GPIO_Pin = GPIO_Pin_2;
    GPIO_InitX.GPIO_Mode = GPIO_Mode_AIN;        
    GPIO_InitX.GPIO_PuPd = GPIO_PuPd_NOPULL ;
    GPIO_Init(GPIOC, &GPIO_InitX);        
}
ADC使用前需进行初始化设置，主包含Common参数配置与ADC的init设置：

Common参数设置主要如下：ADC采用独立工作模式，预分频为4分频，不使用DMA，两通道间隔为20Cycles(当前只使用一个通道，此值可任意配置)

Init参数设置如下：ADC分辩率为12bit，不启用连续转换，不启用多通道扫描模式，不使用硬件触发。数据右对齐，1个转换通道

初始化源码实现如下：

【代码7-2-2】BSP/mq.c

void MQ_ADC_Config(void){
    ADC_InitTypeDef ADC_InitX;
    ADC_CommonInitTypeDef ADC_CommonInitX;
    
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_ADC1 , ENABLE);  // 开启ADC时钟

    ADC_CommonInitX.ADC_Mode      = ADC_Mode_Independent;    // 独立ADC模式
    ADC_CommonInitX.ADC_Prescaler = ADC_Prescaler_Div4;  // 时钟为fpclk x分频    
    ADC_CommonInitX.ADC_DMAAccessMode = ADC_DMAAccessMode_Disabled;  // 禁止DMA直接访问模式    
    ADC_CommonInitX.ADC_TwoSamplingDelay = ADC_TwoSamplingDelay_20Cycles;  //两通道间隔延时
    ADC_CommonInit(&ADC_CommonInitX);
    
    ADC_StructInit(&ADC_InitX);
    ADC_InitX.ADC_Resolution   = ADC_Resolution_12b;    // ADC 分辨率
    ADC_InitX.ADC_ScanConvMode = DISABLE;    // 禁止扫描模式，多通道采集才需要    
    ADC_InitX.ADC_ContinuousConvMode = DISABLE;   //连续转换    
    ADC_InitX.ADC_ExternalTrigConvEdge = ADC_ExternalTrigConvEdge_None;    //禁止外部边沿触发
    ADC_InitX.ADC_ExternalTrigConv = ADC_ExternalTrigConv_T1_CC1;//禁止时可为任意值，无效
    ADC_InitX.ADC_DataAlign = ADC_DataAlign_Right;    //数据右对齐    
    ADC_InitX.ADC_NbrOfConversion = 1;    //转换通道 1个
    ADC_Init(MQ_ADC, &ADC_InitX);
    
    //配置 ADC 通道转换顺序为1，第一个转换
    ADC_RegularChannelConfig(MQ_ADC, ADC_Channel_12, 1, ADC_SampleTime_28Cycles);
    
    ADC_Cmd(MQ_ADC, ENABLE);  // 使能ADC
    
}

void MQ_Init(void){
    MQ_GPIO_Config();
    MQ_ADC_Config();
}
3） ADC数据读取

ADC使用软件触发开始转换，需检测ADC_FLAG_ EOC转换完成标识，当转换完成时读取转换数据，读取的源码实现如下：

【代码7-2-3】BSP/mq.c

uint16_t MQ_ReadValue(void){
    uint32_t uTimeout = 100;
    ADC_SoftwareStartConv(MQ_ADC);//开始ADC转换

    //判断是否采样转换完毕
    while(ADC_GetFlagStatus(MQ_ADC, ADC_FLAG_EOC) != SET){
        if(uTimeout--){  return 0; }
        delay_ms(1);
    }
    ADC_ClearFlag(MQ_ADC, ADC_FLAG_EOC);//清除标志位

    return ADC_GetConversionValue(MQ_ADC);
}
MQ2测试示例实现
1） 创建主程序

MQ2驱动完成后，需在主程序main.c中实现测试示例实现测试，先为主程序创建相关头文件，以便后续使用，源码如下：

【代码7-3-1】USER/main.c

#include <stdio.h>
#include "stm32f4xx.h"
#include "mq.h"
#include "usart.h"
#include "delay.h"
2） 测试示例源码实现

MQ2数据读取，在初始化后，循环读取ADC值，并转换为电压值，完整源码如下：

【代码7-3-2】USER/main.c

/**
  * @brief  主函数
  * @param  无
  * @retval 无
  */
int main(void){
    uint16_t uValue = 0;
    float fVolValue = 0.0;
    
    UART2_Init( 115200 );
    MQ_Init();
    printf("MQ Init OK\r\n");

    while(1){
        uValue = MQ_ReadValue();//读取ADC值
        fVolValue = (float)uValue/4096*(float)3.3;//转换为电压值
        printf("AD=%04d, %fV\r\n", uValue, fVolValue); 
        
        delay_ms( 1000 );  
    }
}
四、实验结果
1） 系统编译烧写运行后，调试串口可见输出ADC初始化成功，MQ2电压循环采集输出，使用向MQ2钢网上哈气，或者拔掉MQ2，可见MQ2电压值变化。

本身该实验需要刚熄灭的打火机来检测燃气，但是考虑到实验室安全问题，不再使用打火机，下图展示使用打火机燃气检测的结果：



图 2 MQ2正常接入（燃气检测到）



图 3 MQ2去掉打火机的结果

 